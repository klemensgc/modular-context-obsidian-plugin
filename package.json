{
  "name": "modular-context",
  "version": "2.1.2",
  "private": true,
  "description": "Obsidian plugin + MCP server for LLM knowledge base and Google Workspace integration",
  "workspaces": [
    "packages/shared",
    "packages/plugin",
    "packages/mcp-google"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "echo \"No tests specified\" && exit 0",
    "lint": "eslint packages/**/src --ext .ts,.tsx"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "esbuild": "^0.20.0"
  }
}