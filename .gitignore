# npm
node_modules/

# Don't include compiled bundle files in the repo (GitHub 100MB limit applies).
# They should be uploaded to GitHub releases as assets instead.
main.js
packages/plugin/mcp-server.js
packages/mcp-google/dist/

# Exclude sourcemaps
*.map

# obsidian
data.json

# Exclude macOS Finder (System Explorer) View States
.DS_Store

# Ralph loop progress files (re-created each run)
_progress.md
_progress-*.md

# Nested experimental projects (separate repos — not part of monorepo)
internetvin-terminal/
modular-context-skills/
obsidian plugin screenshots/

# Electron app build outputs
packages/app/dist/
packages/app/dist-app/

# Google OAuth client secret
packages/plugin/.env.local
